/**
 * GitHub repository branch configuration.
 *
 * Update these constants when upstream branches change so every
 * generated link in the docs stays correct automatically.
 */

/** Default branch of Fancy-Mumble/mumble-docker. */
export const MUMBLE_DOCKER_BRANCH = 'master';

/** Active development branch of Fancy-Mumble/mumble-server. */
export const MUMBLE_SERVER_BRANCH = '1.6.x';
